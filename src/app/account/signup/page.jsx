import { Routes } from "@/constants/Routes";

export const metadata = {
    title: Routes.SIGNUP.meta.title,
    description: Routes.SIGNUP.meta.description,
};

const page = () => {
    return (
        <div>Account - Signup</div>
    )
}

export default page